[3주차]

- 명시적 바인딩 1 (call, apply)
    
    **[this에 별도의 대상을 바인딩하는 방법]**
    
    **[call 메서드]**
    
    : 메서드의 호출 주체인 함수를 즉시 실행하도록 하는 명령어
    
    ```jsx
    Function.prototype.call(thisArg[, arg1[, arg2[, ...]]])
    ```
    
    - 첫번째 인자를 this로 바인딩
    - 이후의 인자들을 호출할 함수의 매개변수로 지정.
    
    ⇒ 함수를 그냥 실행하면 this는 전역객체를 참조하지만, call 메서드를 이용하면 임의의 객체를 this로 지정
    
    (예시)
    
    ```jsx
    var func = function (a,b,c) {
    	console.log(this, a, b, c);
    };
    
    func(1,2,3); //Window{ ...} 1 2 3
    func.call({x:1}, 4,5,6}; // {x:1} 4 5 6
    ```
    
    **[apply 메서드]**
    
    ```jsx
    Function.prototype.apply(thisArg[, argsArray])
    ```
    
    - : call 메서드와 기능적으로는 완전히 동일
    - 두 번째 인자를 배열로 받아 그 배열의 요소들을 호출할 함수의 매개변수로 지정
    
    (에시)
    
    ```jsx
    var func = function (a,b,c) {
    	console.log(this, a, b, c);
    };
    
    func.apply({x:1}, [4, 5, 6]); //{x:1} 4 5 6
    
    ```
    
    [활용]
    
    [1-유사배열객체에서의 활용]
    
    (유사배열객체: 키가 0 또는 양의 정수인 프로퍼티가 존재하고 length 프로퍼티 값이 0 또는 양의 정수인 객체, 즉 배열의 구조와 유사한 객체)
    
    - call 또는 apply 메서드를 이용해 배열 메서드를 차용할 수 있다.
    
    ```jsx
    var obj = {
      0: 'a',
      1: 'b',
      2: 'c',
      length: 3
    };
    
    Array.prototype.push.call(obj, 'd');
    console.log(obj); // { 0: 'a', 1: 'b', 2: 'c', 3: 'd', length: 4 }
    
    var arr = Array.prototype.slice.call(obj);
    console.log(arr); // ['a', 'b', 'c', 'd']
    ```
    
    👉 배열 메서드를 call로 빌려와서, 유사 배열 객체를 배열처럼 다룬 것
    
    👉call/apply 메서드를 이용해 모든 배열 메서드를 적용할 수 있다. 
    
    ES6문법-Array.from 메서드 도입
    
    - 유사배열객체 또는 순회 가능한 모든 종류의 데이터 타입을 배열로 전환
    
    ```jsx
    var obj = {
      0: 'a',
      1: 'b',
      2: 'c',
      length: 3
    };
    
    var arr = Array.from(obj);
    console.log(arr); // ['a', 'b', 'c']
    ```
    
    [2-생성자 내부에서 다른 생성자를 호출]
    
    -생성자 내부에 다른 생성자와 공통된 내용이 있을 경우 call 또는 apply를 이용해 다른 생성자를 호출하면 간단하게 반복을 줄일 수 있다. 
    
    ```jsx
    //예제 3-21
    
    function Person(name, gender) {
      this.name = name;
      this.gender = gender;
    }
    
    function Student(name, gender, school) {
      Person.call(this, name, gender);
      this.school = school;
    }
    
    function Employee(name, gender, company) {
      Person.apply(this, [name, gender]);
      this.company = company;
    }
    
    var by = new Student('보영', 'female', '단국대');
    var jn = new Employee('재남', 'male', '구글');
    ```
    
    -apply를 사용해서 배열을 펼쳐서 받는다.
    
    [3-여러 인수를 묶어 하나의 배열로 전달하고 싶을 때]
    
    ```jsx
    //예제 3-23
    
    var numbers = [10, 20, 3, 16, 45];
    
    var max = Math.max.apply(null, numbers);
    var min = Math.min.apply(null, numbers);
    
    console.log(max, min); // 45 3
    ```
    
    ES6문법-펼치기 연산자 사용
    
    ```jsx
    const numbers = [10, 20, 3, 16, 45];
    
    const max = Math.max(...numbers);
    const min = Math.min(...numbers);
    
    console.log(max, min); // 45 3
    ```
