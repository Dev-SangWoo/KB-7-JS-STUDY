# 4주차

# 콜백 지옥

콜백 지옥 : 콜백 함수를 익명 함수로 전달하는 과정이 반복되어 코드가 너무 깊어지는 현상.

주로 **이벤트 처리, 서버 통신**과 같은 **비동기 작업**에서 나타남.

### 비동기

현재 실행 중인 코드의 완료 여부와 무관하게 다음 코드로 넘어가서 실행

별도의 요청, 실행 대기, 보류 등

- setTimeout : 사용자의 요청에 의해 특정 시간이 경과되기 전까지 어떤 함수의 실행을 보류
- addEventListener : 사용자의 직접적인 개입이 있을 때 비로소 어떤 함수를 실행하도록 대기
- XMLHttpRequest : 웹브라우저 자체가 아닌 별도의 대상에 무언가를 요청하고 그에 대한 응답이 왔을 때 비로소 어떤 함수를 실행하도록 대기

**콜백지옥의 예시**

```jsx
setTimeout(function (name) {
	var coffeeList = name;
	console. log(coffeeList);

	setTimeout(function (name) {
			coffeeList += ', ' + name;
			console. log (coffeeList);
	
			setTimeout (function (name) {
				coffeeList += ', ' + name;
				console. log(coffeeList);

				setTimeout(function (name) 1
					coffeeList + = ' , ' + name;
					console. log(coffeeList);
				}, 500, ' 카페라떼 ');
			}, 500, ' 카페모카 ');
	 }, 500, ' 아메리카노 ');
}, 50, 에스프레소 ')
```

**해결 방법**

익명의 콜백 함수를 모두 기명함수로 전환하기. 하지만, 일회성 함수들을 모두 변수에 할당하고 코드를 계속 따라다니며 읽어야하는 것이 오히려 헷갈릴 수도 있다.

→ 이를 해결하기 위해 Promise, Generator, async/await 등이 도입됨.

```jsx
var coffeeList = "*;

var addEspresso = function (name) {
	coffeeList = name;
	console. log (coffeeList);
	setTimeout(addAmericano, 500, ' 아메리카노 ');
};

var addAmericano = function (name) {
	coffeeList += ' , ' + name;
	console. log (coffeeList);
	setTimeout(addMocha, 500, ' 카페모카 ');
}:

var addMocha = function (name) {
	coffeeList +=', ' + name;
	console. log (coffeeList);
	setTimeout(addLatte, 500, ' 카페라떼 ');
}:

var addLatte = function (name) {
	coffeeList + = ' '+ name
	console. log(coffeeList);
setTimeout(addEspresso, 500, ' 에스프레소');
```

### 비동기 작업의 동기적 표현1 - Promise(1)

new 연산자와 함께 호출한 Promise가 넘겨주는 콜백 함수는 바로 실행되지만, 

그 내부에 resolve나 reject 함수를 호출하면 둘 중 하나가 실행되기 전까지는 then이나 catch로 넘어가지 않음. 

→ 비동기 작업이 완료될 때 resolve나 reject를 호출하는 방법으로 동기적 표현이 가능해짐. 

```jsx
new Promise(function (resolve) {
	setTimeout (function () {
		var name ='에스프레소';
		console. log (name);
		resolve (name) ;
	}, 500);
}). then(function (prevName) {
	return new Promise(function (resolve) {
		setTimeout (function () {
			var name = prevName + ', 아메리카노 ';
			console. log (name);
			resolve (name);
		}, 500);
	});
}). then(function (prevName) {
	return new Promise(function (resolve) {
		setTimeout(function () {
			var name = prevName + ', 카페모카 ';
			console. log (name);
			resolve (name);
		}, 500);
	});
}). then(function (prevName) {
	return new Promise(function (resolve) {
		setTimeout (function () {
			var name = prevName + ', 카페라떼 ';
			console. log (name);
			resolve (name);
		}, 500):
	});
});
```